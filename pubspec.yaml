name: interactive_tables
description: "A new Flutter project."
version: 0.0.1
homepage: https://github.com/GeorgeSWalker/interactive_tables.git

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_form_builder: ^9.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

