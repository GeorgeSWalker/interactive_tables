name: power_table
description: A powerful and flexible Flutter package for creating feature-rich, interactive data tables with sorting, pagination, and more.
version: 0.0.1
homepage: https://github.com/georgeswalker/power_table
repository: https://github.com/georgeswalker/power_table.git
issue_tracker: https://github.com/georgeswalker/power_table/issues

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  linked_scroll_controller: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
